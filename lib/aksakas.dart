import 'package:flutter/material.dart';

class Aksakas extends StatelessWidget {
  const Aksakas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}